= render 'partials/header'
section
    div id="filter"
        = render 'partials/filterOptions'

        - if @games.size.zero?
            p No Results Found.
        - else
            = render 'partials/boardgames'
